logging:
  level:
    root: info
    org:
      springframework.web: debug
      hibernate: error

---

spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: info
    org:
      springframework: info
      springframework.web: debug
      springframework.boot.autoconfigure.security: warn
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace
